import React, { useState, useRef } from 'react'

const TimePicker = ({ renderInput }) => {

    return (
        <div className='w-screen h-screen fixed z-[1000] bg-[#00000063] top-0 left-0 flex items-center justify-center'>
            
        </div>
    )
}

export default TimePicker